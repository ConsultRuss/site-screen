# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Virtual environments (project-local; deps are pinned in pyproject) ---
.venv/
venv/
env/

# --- Node / Cloudflare Worker ---
node_modules/
.wrangler/

# --- Secrets / local config (never commit) ---
.env
.dev.vars
*.key
*.pem
secrets/

# --- Cached raw geodata (large; regenerated by the pipeline) ---
# NOTE: exported web/data/*.geojson IS committed — the static site serves it.
# screen_run.json is regenerated provenance (timestamped); not tracked.
data/raw/
data/cache/
data/screen_run.json
*.tif
*.tiff
*.gpkg
*.zip
*.shp
*.shx
*.dbf
*.prj
*.sbn
*.sbx
*.cpg

# --- Editor / OS / agent scratch ---
.idea/
.vscode/
.DS_Store
Thumbs.db
.claude/
