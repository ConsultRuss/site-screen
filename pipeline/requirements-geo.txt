# Geospatial stack for the fetch/build pipeline stages. Not needed to run the web
# app or the score/export commands — install it only to rebuild from source data.
#
# Every dependency is pinned to an EXACT version. Before bumping any pin, confirm the
# PyPI artifact matches the project's GitHub tag and read the changelog; these wheels
# are large, platform-sensitive, and easy to break as a set.
#
# Note: the project uses its OWN virtual environment; these pins are independent
# of whatever is installed system-wide. pandas is held at 2.x here for geopandas
# compatibility regardless of any newer system pandas.

geopandas==1.0.1
shapely==2.0.6
pyproj==3.6.1
fiona==1.10.1
rasterio==1.4.3
rasterstats==0.20.0
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
